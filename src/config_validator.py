import json

# def check_dependencies():
    #TODO check dependencies

    # f = open("../config/config.json")
    # data = json.load(f)
    #
    # if next((item for item in data["tabs"] if item["tab"] == "person" and item["exist"] == False), None):
    #     if next((item for item in data["tabs"] if (item["tab"] == "client" or item["tab"] == "employee")
    #                                               and item["exist"] == True), None):
    #         raise Exception("Cannot provide Client or Employee table without Person table reference!")
    #
    # f.close()